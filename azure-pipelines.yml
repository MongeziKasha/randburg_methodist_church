trigger:
  branches:
    include:
      - '*'  # Runs pipeline on push to ANY branch

pr:
  branches:
    include:
      - main  # Runs pipeline when a PR is created for ANY branch targeting 'main'

pool:
  vmImage: 'ubuntu-latest'

variables:
  buildConfiguration: 'Release'
  solution: 'randburg_methodist_church.sln'

steps:
- name: Checkout code
  uses: actions/checkout@v3

- task: UseDotNet@2
  inputs:
    packageType: 'sdk'
    version: '7.x'  # Change this to your .NET version

- name: Restore dependencies
  run: dotnet restore $(solution)

- name: Build the solution
  run: dotnet build $(solution) --configuration $(buildConfiguration) --no-restore

- name: Run unit tests
  run: dotnet test randburg_methodist_church_unitTests/randburg_methodist_church_unitTests.csproj --configuration $(buildConfiguration) --no-build

# Uncomment and modify below if you plan to publish the Blazor app later
# - name: Publish Blazor App
#   run: dotnet publish randburg_methodist_church/randburg_methodist_church.csproj -c $(buildConfiguration) -o $(Build.ArtifactStagingDirectory)

# - task: PublishBuildArtifacts@1
#   inputs:
#     pathToPublish: $(Build.ArtifactStagingDirectory)
#     artifactName: 'drop'

# - task: AzureWebApp@1
#   inputs:
#     azureSubscription: 'Your-Azure-Service-Connection'
#     appName: 'your-blazor-app'
#     package: '$(Build.ArtifactStagingDirectory)/**/*.zip'